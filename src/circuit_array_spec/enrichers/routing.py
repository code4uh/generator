"""Routing enricher placeholder."""

from __future__ import annotations


def enrich_routing(spec: dict) -> dict:
    """Return spec unchanged until routing enrichment is implemented."""
    return spec
